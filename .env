# Optional. Without it the app still works — Share falls back to attaching the
# file directly, which is the path almost every mobile user takes anyway.
# With it, sharing produces a link whose preview shows the generated graphic.
BLOB_READ_WRITE_TOKEN=store_QVyowLOXoKvER855
